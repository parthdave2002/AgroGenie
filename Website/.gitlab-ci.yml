image: node:22.21.1

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - cp dist/index.html dist/404.html
  artifacts:
    paths:
      - dist
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
  pages:
    publish: dist
